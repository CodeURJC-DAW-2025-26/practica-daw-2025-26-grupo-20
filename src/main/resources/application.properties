spring.application.name=mokaf
#server.port=8080  ya no es necesario pues tengo los puertos 443 para las ramas y dev y 8443 para main

# =========================
# MySQL datasource
# =========================
spring.datasource.url=jdbc:mysql://localhost:3306/mokaf_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Europe/Madrid
spring.datasource.username=root
spring.datasource.password=URJC2019!

# (opcional) pool/driver
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# =========================
# JPA / Hibernate
# =========================
# Usa "create" para forzar la recreación si faltan tablas.
spring.jpa.hibernate.ddl-auto=create


# Logging SQL (útil para depurar)
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# LOB/Blob: reduce problemas con MySQL en algunos entornos
spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true
#spring.profiles.active=dev

#############  HTTPs ################
server.port=8443
server.ssl.key-store=classpath:keystore.jks
server.ssl.key-store-password=password
server.ssl.key-store-type=JKS
server.ssl.key-alias=selfsigned

# Mustache
# spring.mustache.env.process-missing-variables=false
# spring.mustache.compiler.ignore-missing-variables=true

# =========================
# Mail (SMTP) Configuration
# =========================
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=mokafcoffe@gmail.com
spring.mail.password=frrp gtbm vzvl gldg
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true


# File upload configuration
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=5MB
spring.servlet.multipart.max-request-size=10MB

# jwt SECURITY
jwt.secret=mokaf-super-secret-key-must-be-at-least-32-chars